<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>USM Forms</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(45deg, #612ccd 0%, #541170 99%, #8902ff 100%);
            min-height: 100vh;
            padding: 2rem;
        }

        .forms-container {
            background: rgba(255, 255, 255, 0.25);
            backdrop-filter: blur(10px);
            border-radius: 10px;
            padding: 2rem;
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            box-shadow: 0 8px 32px rgba(31, 38, 135, 0.37);
            border: 1px solid rgba(255, 255, 255, 0.18);
            transform: translateY(20px);
            opacity: 0;
            animation: fadeIn 0.5s ease-out forwards;
        }

        .forms-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            color: #fff;
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .user-rank {
            background: rgba(255, 255, 255, 0.1);
            padding: 0.5rem 1rem;
            border-radius: 5px;
            font-size: 0.9rem;
        }

        .nav-menu {
            display: flex;
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .nav-item {
            background: rgba(255, 255, 255, 0.1);
            padding: 0.75rem 1.5rem;
            border-radius: 5px;
            color: white;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .nav-item:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
        }

        .forms-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .form-card {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 2rem;
            color: white;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .form-card:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.2);
        }

        .form-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        .form-title {
            font-size: 1.2rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .form-description {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 1.5rem;
        }

        .form-button {
            background: #4299e1;
            color: white;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 1rem;
            width: 100%;
        }

        .form-button:hover {
            background: #3182ce;
        }

        @keyframes fadeIn {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="forms-container">
        <div class="forms-header">
            <div class="user-info">
                <h2>Forms</h2>
            </div>
        </div>

        <nav class="nav-menu">
            <a href="/dashboard" class="nav-item">Dashboard</a>
            <a href="/members" class="nav-item">Members</a>
            <a href="/forms" class="nav-item">Forms</a>
            <a href="/profile" class="nav-item">Profile</a>
        </nav>

        <div class="forms-grid">
            <div class="form-card">
                <div class="form-icon">📋</div>
                <h3 class="form-title">Training Form</h3>
                <p class="form-description">Submit a training session report or request new training</p>
                <button class="form-button" onclick="window.location.href='/forms/training'">
                    Training Form
                </button>
            </div>

            <div class="form-card">
                <div class="form-icon">⭐</div>
                <h3 class="form-title">Promotion Form</h3>
                <p class="form-description">Submit a promotion request or recommendation</p>
                <button class="form-button" onclick="window.location.href='/forms/promotion'">
                    Promotion Form
                </button>
            </div>

            <div class="form-card">
                <div class="form-icon">🏆</div>
                <h3 class="form-title">Award Form</h3>
                <p class="form-description">Submit an award recommendation or request</p>
                <button class="form-button" onclick="window.location.href='/forms/award'">
                    Award Form
                </button>
            </div>

            <% 
            const officerRanks = [
                'Second Lieutenant',
                'First Lieutenant',
                'Captain',
                'Major',
                'Lieutenant Colonel',
                'Colonel',
                'Brigadier General',
                'Major General',
                'Lieutenant General',
                'General',
                'General of the Army'
            ];
            
            const hasOfficerRole = user.roles && user.roles.some(role => 
                officerRanks.includes(role.name)
            );

            if (hasOfficerRole) { %>
                <div class="form-card">
                <div class="form-icon">✅</div>
                <h3 class="form-title">Pending Approvals</h3>
                <p class="form-description">Review and manage pending training approvals</p>
                <button class="form-button" onclick="window.location.href='/forms/approvals'">
                    Pending Approvals
                </button>
            </div>

                <div class="form-card">
                <div class="form-icon">📝</div>
                <h3 class="form-title">All Trainings</h3>
                <p class="form-description">View all training records</p>
                <button class="form-button" onclick="window.location.href='/forms/alltrainings'">
                    All Trainings
                </button>
            </div>

            <div class="form-card">
                <div class="form-icon">📋</div>
                <h3 class="form-title">Pending Promotions</h3>
                <p class="form-description">Review and handle pending promotion requests</p>
                <button class="form-button" onclick="window.location.href='/forms/pendingpromotions'">
                    Pending Promotions
                </button>
            </div>

            <div class="form-card">
                <div class="form-icon">📍</div>
                <h3 class="form-title">Placement Form</h3>
                <p class="form-description">Submit unit placement requests</p>
                <button class="form-button" onclick="window.location.href='/forms/placement'">
                    Placement Form
                </button>
            </div>

            <div class="form-card">
                <div class="form-icon">🏆</div>
                <h3 class="form-title">Pending Awards</h3>
                <p class="form-description">Review and manage pending awards</p>
                <button class="form-button" onclick="window.location.href='/forms/pendingawards'">
                    Pending Awards
                </button>
            </div>

            <div class="form-card">
                <div class="form-icon">⚔️</div>
                <h3 class="form-title">Commission Form</h3>
                <p class="form-description">Commission a member</p>
                <button class="form-button" onclick="window.location.href='/forms/commission'">
                    Commission Form
                </button>
            </div>

            <div class="form-card">
                <div class="form-icon">📋</div>
                <h3 class="form-title">All Promotions</h3>
                <p class="form-description">View all promotion records</p>
                <button class="form-button" onclick="window.location.href='/forms/allpromotions'">
                    All Promotions
                </button>
            </div>
        <% } %>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const navItems = document.querySelectorAll('.nav-item');
            navItems.forEach(item => {
                item.addEventListener('mouseenter', () => {
                    item.style.transform = 'translateY(-3px)';
                    item.style.boxShadow = '0 4px 6px rgba(50, 50, 93, 0.11)';
                });
                item.addEventListener('mouseleave', () => {
                    item.style.transform = 'translateY(0)';
                    item.style.boxShadow = 'none';
                });
            });
        });
    </script>
</body>
</html>