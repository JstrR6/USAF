<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>USAF Dashboard - <%= title %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <!-- Sidebar -->
    <div class="min-h-screen flex">
        <div class="bg-blue-900 text-white w-64 py-4 flex-shrink-0">
            <div class="px-6 py-4 border-b border-blue-800">
                <h2 class="text-2xl font-bold">USAF Dashboard</h2>
            </div>
            <nav class="mt-4">
                <a href="/dashboard" class="block px-6 py-2 hover:bg-blue-800 <%= path === '/dashboard' ? 'bg-blue-800' : '' %>">
                    <i class="fas fa-home mr-2"></i> Dashboard
                </a>
                <a href="/forms" class="block px-6 py-2 hover:bg-blue-800 <%= path === '/forms' ? 'bg-blue-800' : '' %>">
                    <i class="fas fa-file-alt mr-2"></i> Forms
                </a>
                <a href="/members" class="block px-6 py-2 hover:bg-blue-800 <%= path === '/members' ? 'bg-blue-800' : '' %>">
                    <i class="fas fa-users mr-2"></i> Members
                </a>
                <a href="/profile" class="block px-6 py-2 hover:bg-blue-800 <%= path === '/profile' ? 'bg-blue-800' : '' %>">
                    <i class="fas fa-user mr-2"></i> Profile
                </a>
            </nav>
        </div>

        <!-- Main Content -->
        <div class="flex-1">
            <!-- Top Navigation -->
            <header class="bg-white shadow">
                <div class="container mx-auto px-6 py-4 flex justify-between items-center">
                    <h1 class="text-2xl font-semibold text-gray-800"><%= title %></h1>
                    <div class="flex items-center">
                        <% if (user) { %>
                            <div class="mr-4">
                                <span class="text-sm text-gray-600">Rank:</span>
                                <span class="ml-1 font-semibold"><%= highestRole %></span>
                            </div>
                            <div class="relative">
                                <button class="flex items-center focus:outline-none">
                                    <span class="ml-2"><%= user.username %></span>
                                </button>
                            </div>
                        <% } %>
                    </div>
                </div>
            </header>

            <!-- Page Content -->
            <main class="container mx-auto px-6 py-8">
                <%- body %>
            </main>
        </div>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
</body>
</html>